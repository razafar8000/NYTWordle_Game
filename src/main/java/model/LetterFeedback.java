package model;

// LetterFeedback.java
// Enum defining the three possible feedback states for each letter in a guess:
// CORRECT (green), PRESENT (yellow), or ABSENT (gray).

public enum LetterFeedback {
    CORRECT, PRESENT, ABSENT
}
